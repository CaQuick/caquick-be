/** realtimeBestCakes 기본 카드 수(figma 검색 진입 시안 리스트 기준). */
export const DEFAULT_REALTIME_BEST_LIMIT = 10;

/** realtimeBestCakes 최대 카드 수. */
export const MAX_REALTIME_BEST_LIMIT = 20;

/** '실시간' 판매 집계 윈도우(시간). 사용자 확정: 최근 24시간 주문 수량 합. */
export const REALTIME_BEST_WINDOW_HOURS = 24;
