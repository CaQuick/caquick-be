introspection:
  url: http://localhost:4000/graphql
  headers:
    Authorization: "Bearer ${SPECTAQL_ACCESS_TOKEN}"

info:
  title: CaQuick GraphQL API
  description: CaQuick GraphQL API 문서
  version: 0.0.1

servers:
  - url: http://localhost:4000/graphql
    description: Local

output:
  dir: docs/graphql
  reportFileName: index.html
